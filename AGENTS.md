# Project Instructions

Mighty Memequake and Sir Mudflap Goblin are building a pi custom provider for NewAPI Gateway.

- Keep the provider OpenAI-compatible unless NewAPI-specific API behavior requires custom streaming.
- Prefer small changes in `index.ts`; avoid adding runtime dependencies unless the gateway API requires them.
- Keep generated docs in English unless requested otherwise.
